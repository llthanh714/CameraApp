<div class="d-flex flex-column h-100 p-3">
    <div class="navbar-brand d-flex align-items-center mb-4 px-2">
        <span class="fs-4 fw-bold" style="background: var(--primary-gradient); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">
            Gemini Cam
        </span>
    </div>

    <div class="nav flex-column nav-pills gap-2">
        <div class="nav-item">
            <NavLink class="nav-link d-flex align-items-center gap-3" href="" Match="NavLinkMatch.All">
                <i class="bi bi-house-door"></i> Trang chủ
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link d-flex align-items-center gap-3" href="camera">
                <i class="bi bi-camera-video"></i> Studio Quay
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link d-flex align-items-center gap-3" href="settings">
                <i class="bi bi-gear"></i> Cài đặt
            </NavLink>
        </div>
    </div>

    <div class="mt-auto px-2 pb-2">
        <div class="text-muted small">© 2024 Gemini Style</div>
    </div>
</div>

<style>
    .nav-link {
        color: var(--text-secondary);
        border-radius: var(--radius-md);
        padding: 12px 16px;
        transition: all 0.2s;
    }

        .nav-link:hover {
            background-color: var(--bg-surface-hover);
            color: var(--text-primary);
        }

        .nav-link.active {
            background-color: rgba(66, 133, 244, 0.15);
            color: var(--accent-blue);
            font-weight: 600;
        }

    .bi {
        font-size: 1.2rem;
    }
</style>